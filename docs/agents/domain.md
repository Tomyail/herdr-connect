# 领域文档使用规则

本仓库采用 single-context 布局。

## 探索代码前必须阅读

- 根目录 `CONTEXT.md`：Herdr Connect 统一领域语言。
文件不存在时直接继续，不要预先创建空文档。领域词汇形成后，再通过领域建模流程补充。

## 统一语言

issue、spec、ticket、测试名称和评审结论必须使用 `CONTEXT.md` 已定义的术语。若需要的新概念尚未定义，应先判断它是领域缺口还是不必要的同义词，不能静默发明新叫法。
